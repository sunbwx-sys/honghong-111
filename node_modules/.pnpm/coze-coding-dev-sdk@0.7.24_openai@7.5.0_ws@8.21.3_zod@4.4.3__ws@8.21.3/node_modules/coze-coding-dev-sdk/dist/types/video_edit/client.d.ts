import { BaseClient } from '../core/client';
import { Config } from '../core/config';
import { FrameExtractorResponse, VideoEditResponse, SubtitleConfig, TextItem, OutputSync, SubtitleType, AudioFormat } from './models';
export declare class FrameExtractorClient extends BaseClient {
    private baseUrl;
    constructor(config?: Config, customHeaders?: Record<string, string>, verbose?: boolean);
    extractByKeyFrame(url: string): Promise<FrameExtractorResponse>;
    extractByInterval(url: string, intervalMs: number): Promise<FrameExtractorResponse>;
    extractByCount(url: string, count: number): Promise<FrameExtractorResponse>;
}
export declare class VideoEditClient extends BaseClient {
    private baseUrl;
    constructor(config?: Config, customHeaders?: Record<string, string>, verbose?: boolean);
    private validateUrlExpire;
    addSubtitles(video: string, subtitleConfig: SubtitleConfig, options?: {
        subtitleUrl?: string;
        textList?: TextItem[];
        urlExpire?: number;
    }): Promise<VideoEditResponse>;
    concatVideos(videos: string[], options?: {
        transitions?: string[];
        urlExpire?: number;
    }): Promise<VideoEditResponse>;
    compileVideoAudio(video: string, audio: string, options?: {
        isVideoAudioSync?: boolean;
        outputSync?: OutputSync;
        isAudioReserve?: boolean;
        urlExpire?: number;
    }): Promise<VideoEditResponse>;
    audioToSubtitle(source: string, options?: {
        subtitleType?: SubtitleType;
        urlExpire?: number;
    }): Promise<VideoEditResponse>;
    extractAudio(video: string, options?: {
        format?: AudioFormat;
        urlExpire?: number;
    }): Promise<VideoEditResponse>;
    videoTrim(video: string, options?: {
        startTime?: number;
        endTime?: number;
        urlExpire?: number;
    }): Promise<VideoEditResponse>;
}
//# sourceMappingURL=client.d.ts.map