import type { NextHandleFunction } from 'connect';
import type { RequestHandler } from 'express';
export declare const launchEditorMiddleware: NextHandleFunction;
export declare const createLaunchEditorMiddleware: (() => RequestHandler<any>);
